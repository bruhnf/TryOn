# Fail2Ban filter for PHP file requests (we don't serve PHP)
# Instant ban for known malicious PHP probes

[Definition]

# Match any request for .php files
failregex = ^<HOST> .* "(GET|POST) /[^"]*\.php

ignoreregex = 

# Author: TryOn Security
